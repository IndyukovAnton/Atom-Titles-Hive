# Database folder for SQLite
This folder contains the SQLite database file `app.db` which is created automatically when the application starts.

The database file is excluded from git via .gitignore.
