# Задача 02: Очистка и оптимизация CSS-переменных

## Описание проблемы
В файле `src/index.css` обнаружено:
- Дублирование переменных для тем (`:root` и `[data-theme="dark"]` + `.dark`)
- Смешение устаревших переменных (`--bg-primary`, `--text-primary`) с современными oklch-токенами
- Неиспользуемые CSS-переменные
- Несогласованность механизмов переключения тем

## Цель задачи
Привести CSS-переменные к единому стандарту, удалить дублирование и обеспечить чёткий механизм переключения тем.

## Чек-лист выполнения

### Этап 1: Аудит переменных
- [x] Создать список всех CSS-переменных в проекте
- [x] Определить используемые и неиспользуемые переменные
- [x] Проверить, какой механизм темы используется (`[data-theme]` или `.dark`)
- [x] Найти все места использования переменных в компонентах

### Этап 2: Выбор стандарта цветов
- [x] Определить: использовать oklch ИЛИ rgba/hsl
- [x] Создать единую цветовую палитру (light/dark)
- [x] Документировать выбранный подход в `docs/architecture/color-system.md`

### Этап 3: Рефакторинг index.css
- [x] Удалить дублированные переменные для темной темы
- [x] Выбрать один механизм: `[data-theme="dark"]` ИЛИ `.dark`
- [x] Привести все переменные к единому формату
- [x] Удалить неиспользуемые переменные (`--btn-primary-bg-hover`, `--icon-color` и т.д. если не используются)
- [x] Группировать переменные логически (цвета, отступы, радиусы и т.д.)
- [x] Добавить комментарии к группам переменных

### Этап 4: Обновление ThemeContext
- [x] Проверить `src/contexts/ThemeContext.tsx` на соответствие с выбранным механизмом
- [x] Убедиться, что переключение темы работает только через один механизм
- [x] Удалить устаревшие обработчики смены темы

### Этап 5: Обновление компонентов
- [x] Найти все использования устаревших переменных в компонентах
- [x] Заменить на новые стандартизированные переменные
- [x] Проверить shadcn/ui компоненты на использование правильных токенов

### Этап 6: Оптимизация @theme inline
- [x] Проверить необходимость всех radius-переменных
- [x] Убедиться, что все sidebar-переменные используются
- [x] Удалить неиспользуемые chart-переменные (если не нужны)

## Критерии приёмки
- ✅ Отсутствует дублирование CSS-переменных
- ✅ Используется единый механизм переключения тем
- ✅ Все переменные документированы
- ✅ Код чистый и читаемый
- ✅ Размер index.css уменьшен минимум на 20%

## Тестирование

### Визуальная проверка тем
- [x] Светлая тема отображается корректно на всех страницах
- [x] Темная тема отображается корректно на всех страницах
- [x] Переключение между темами происходит плавно
- [x] Все цвета соответствуют дизайн-системе
- [x] Контрастность текста достаточна (WCAG AA)

### Проверка компонентов
- [x] Кнопки имеют правильные цвета в обеих темах
- [x] Формы и инпуты корректно окрашены
- [x] Модальные окна используют правильные цвета фона
- [x] Границы элементов видны и согласованы
- [x] Тени элементов адаптируются к темам

### Функциональная проверка
- [x] Переключатель темы работает
- [x] Выбранная тема сохраняется после перезагрузки страницы
- [x] При первом посещении применяется системная тема (если реализовано)

### Проверка кода
- [x] ESLint не выдаёт предупреждений
- [x] TypeScript компилируется без ошибок
- [x] CSS валиден (можно проверить через stylelint)
- [x] Нет неиспользуемых переменных
- [x] `npm run build` проходит успешно

### Проверка производительности
- [x] Размер CSS bundle уменьшился
- [x] Скорость переключения темы < 100ms
- [x] Нет мерцания при смене темы

## ⚠️ ВАЖНО: Финальная проверка
После выполнения всех чек-боксов:
1. ✅ Все пункты отмечены
2. ✅ Проведено полное тестирование в обеих темах
3. ✅ Код ревью пройдено (самопроверка кода)
4. ✅ Production build создан успешно
5. ✅ Приложение работает стабильно
6. ✅ Документация обновлена

**ТОЛЬКО ПОСЛЕ ЭТОГО** перенести файл в `docs/tasks/done/02-cleanup-css-variables.md`
